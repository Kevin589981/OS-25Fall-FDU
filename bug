在syscall打印出x0 is 400000000, context x0 is 400000000，但是在测试的地方打印出USER: p's address is 0x0，然后我经过gdb调试，在trap.S运行完毕时，x0还是400000000，然后我发现很神奇的是这里返回的是32位可以表示的数的话，USER: p's address is打印的就是同一个数，如果是32位不能表示的，就打印的是0，所以我把地址空间改为了0x40000000（少了一个0，于是32位可以表示），然后现在mmaptest前面几个函数跑通了，


问题不在 munmap，而是在 sys_wait4 参数顺序与用户态调用不一致，触发了无限自旋，导致父进程占用 CPU，子进程看起来“卡住”。已修正参数顺序并去掉死循环，同时写回退出码。


